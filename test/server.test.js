const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');
test('frontend assets and server entry exist',()=>{for(const f of ['server.js','public/index.html','public/app.css','public/app.js'])assert.equal(fs.existsSync(path.join(__dirname,'..',f)),true)});
test('server limits proxied Comfy routes',()=>{const source=fs.readFileSync(path.join(__dirname,'..','server.js'),'utf8');assert.match(source,/const allowed = new Set/);assert.match(source,/Unknown API route/)});
test('persistent library and agent APIs are present',()=>{const source=fs.readFileSync(path.join(__dirname,'..','server.js'),'utf8');assert.match(source,/\/api\\\/library/);assert.match(source,/\/api\/agent\/generate/);assert.match(source,/LIBRARY_FILE/)});
test('expanded frontend modules are shipped',()=>{for(const f of ['public/features.css','public/enhancements.js','skills/comfy-studio-image/SKILL.md'])assert.equal(fs.existsSync(path.join(__dirname,'..',f)),true)});
test('LLM connection test keeps credentials server-side',()=>{const source=fs.readFileSync(path.join(__dirname,'..','server.js'),'utf8');assert.match(source,/\/api\/llm\/test/);assert.match(source,/hasLlmApiKey:Boolean\(llmApiKey\)/)});
test('negative prompt sanitizer removes positive tags and LoRA leakage',()=>{const {sanitizeNegativePrompt}=require('../server');const positive='2girls, long silver hair, celestial anime art style, <lora:aster:1>';const negative=sanitizeNegativePrompt(positive,'multiple girls, silver hair, celestial anime art style, <lora:aster:1>, bad hands, watermark, bad hands',[{trigger:'aster'}]);assert.equal(negative,'bad hands, watermark')});
test('negative prompt sanitizer removes semantic background and eye conflicts',()=>{const {sanitizeNegativePrompt}=require('../server');assert.equal(sanitizeNegativePrompt('simple unobtrusive background, both eyes open and visible','plain background, closed eyes, bad hands'),'bad hands')});
test('LoRA trigger is inferred from embedded training tags',()=>{const {inferLoraTrigger}=require('../server');const metadata={ss_datasets:JSON.stringify([{tag_frequency:{img:{aster_vale:529,'1girl':529,'silver hair':520}}}])};assert.equal(inferLoraTrigger('aster-vale-illustriousxl.safetensors',metadata),'aster_vale')});
test('prompt dialect follows the selected image model family',()=>{const {promptDialectForModel}=require('../server');assert.equal(promptDialectForModel('anima-preview.safetensors').id,'qwen');assert.equal(promptDialectForModel('waiNSFWIllustrious_v140.safetensors').id,'illustrious');assert.equal(promptDialectForModel('ponyDiffusionV6XL.safetensors').id,'pony');assert.equal(promptDialectForModel('FLUX.1-dev.safetensors').id,'flux');assert.equal(promptDialectForModel('realvisxlV50.safetensors').id,'sdxl')});
test('model guidance explains node-loaded LoRAs and exact triggers',()=>{const {promptGuidanceForModel}=require('../server'),guide=promptGuidanceForModel('anima.safetensors').instructions;assert.match(guide,/LoraLoader node/);assert.match(guide,/activation trigger exactly once/);assert.match(guide,/Never emit <lora:name:weight>/)});
test('character resolver requests canon details without embedding user examples',()=>{const source=fs.readFileSync(path.join(__dirname,'..','server.js'),'utf8');assert.match(source,/canonical visual identity brief/);assert.match(source,/body_proportions/);assert.match(source,/default_outfit/);assert.doesNotMatch(source,/verified_overrides/) });
